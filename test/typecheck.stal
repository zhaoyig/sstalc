_f:
    code[$rho]{ sp: $rho, rax: int, rbx: lalal }.
        add rax, rbx
        halt int
_main:
    code[]{ sp: nil }.
        mov rax, $2
        mov rbx, $5
        add rax, rbx
        mov rcx, $6
        jmp _f[nil]