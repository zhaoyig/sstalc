_f:
    code[$rho]{ sp: $rho, rax: int, rbx: int }.
        add rax, rbx
        halt int
_main:
    code[]{ sp: nil }.
        mov rax, $2
        mov rbx, $5
        add rax, rbx
        jmp _f[int :: $rho][lolol]